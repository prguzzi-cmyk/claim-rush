{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'alembic upgrade head 2>&1 || echo Migration_skipped; python -m app.initial_data 2>&1 || echo Initial_data_skipped; uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}'",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE"
  }
}
